# 커밋 컨벤션

ToneFit_FE 커밋 메시지 규칙. 기존 히스토리의 다수 관행(Conventional Commits 어휘 + 한글 제목)을
성문화한 것으로, 새 커밋부터 적용한다. 과거 커밋은 수정하지 않는다.

## 제목 (첫 줄)

```
type: 한글 제목 (V번호)
```

- **타입 접두사 필수.** 소문자 영문 + 콜론 + 공백 한 칸.
- 제목은 **한국어**, 코드 식별자·기술 용어(예: `thinkingConfig`, Flyway)는 원문 그대로.
- **50자 이내**, 마침표 없음, 명사형·개조식 종결 (~반영 / ~정정 / ~추가. "~했습니다" 금지).
- 제목 안에 콜론(`:`)을 다시 쓰지 않는다. 구분이 필요하면 `—` 나 `·` 사용.
- DB 마이그레이션이 포함되면 제목 끝에 `(V번호)` 를 붙인다.

## 타입

| 타입       | 기준                                  | 비고                                         |
| ---------- | ------------------------------------- | -------------------------------------------- |
| `feat`     | 새로운 기능 추가                      |                                              |
| `fix`      | 버그 수정 (잘못된 동작 → 의도된 동작) |                                              |
| `chore`    | 설정, 패키지 등 기타 작업             | 버전 커밋은 `chore: version X.Y.Z` 고정 양식 |
| `refactor` | 코드 구조 개선                        |                                              |
| `style`    | 코드 포맷 변경                        |
| `docs`     | 문서 수정                             |                                              |
| `test`     | 테스트 코드                           |                                              |
| `remove`   | 파일 또는 코드 삭제                   |                                              |

- `release:`, `init:` 등 위 표에 없는 타입은 쓰지 않는다 (버전은 `chore: version X.Y.Z`).
- 여러 타입에 걸치면 **주된 의도** 하나로. 기능과 무관한 수정이 섞이면 커밋을 나눈다 (1 커밋 = 1 논리 변경).

## 본문

제목 다음 빈 줄 하나를 두고, 아래 구조를 따른다 (현행 관행 유지):

1. **배경/원인 문단** — 왜 이 변경이 필요한가. 증상·근본 원인·요청 출처(PM/FE/QA) 포함.
2. **변경점 불릿** — 무엇을 바꿨는가. 파일·클래스 단위로 `- ` 목록.
3. **검증 줄** — 컴파일/테스트/수동 확인 결과 한 줄.

한 줄로 자명한 변경(`chore: version X.Y.Z` 등)은 본문 생략 가능.

## 예시

```
feat: 생성 프롬프트 new4 반영 — 동료·협력사 해요체 강제 강화 (V23)

PM new4 갱신 반영. 변경은 동료(RCP-02)·협력사(RCP-04) 어체 규칙 강화뿐이라
해당 2건만 v1.0 비활성화 → v1.1 시드.

- 해요체 "절대 금지" + 변환 예시 확대 + 작성 직후 자가 점검 지시.
- 상사·고객사는 하십시오체라 무변경.

달러쿼팅 4·행 2 확인, compileJava 통과.
```

```
fix: 작성 요청 questions 필드명을 파악 응답과 일치(question)로 정정
```

나쁜 예: `회신 한도: 1건당 1회 차감으로 되돌림` (타입 없음 + 제목 내 콜론),
`merge test`, `Update README.md` (무의미 제목).

## 브랜치·PR

- 브랜치: `feature/<주제>` / `fix/<주제>` / `deploy/<버전>` (kebab-case 영문).
- PR 병합은 GitHub 기본 머지 커밋(`Merge pull request #N ...`)을 그대로 둔다.
- 배포는 `deploy/X.Y.Z` 브랜치에 `chore: version X.Y.Z` 커밋 후 PR.
- 배포 PR 에 [CHANGELOG.md](CHANGELOG.md) 해당 버전 섹션을 함께 추가한다 (직전 배포 이후 커밋 제목 나열).

## 푸터

- AI 협업 커밋의 `Co-Authored-By:` 푸터는 유지한다.
